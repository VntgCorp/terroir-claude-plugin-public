---
name: onboarding-start
description: Terroir 온보딩 진입점. 직무 확인 → 공통 업무 환경 셋팅 → 직무별 경로 안내를 진행하고, 마지막에 시작 안내(스타트 포인트)와 커넥터 상태별 "할 수 있는 일"을 출력한다. 시작 안내 스킬(플러그인 `terroir-*-guide` 의 스킬, 세션 표기 `terroir-*-guide:*`)이 없는 세션에서는 "내가 무엇을 할 수 있지?" 질문에도 같은 안내를 제공한다.
when_to_use: Terroir를 처음 시작할 때, 온보딩을 진행하고 싶을 때. 트리거 예 "온보딩 시작", "테루아 온보딩 진행해줘", "/onboarding-start". 세션에 시작 안내 스킬(`terroir-*-guide:*` 패턴)이 없는 사용자(= 온보딩 전)가 "테루아 시작하자", "뭐부터 해야 해?"라고 말할 때도 이 스킬이 받는다 — 그 스킬이 세션에 있으면 이 발화들은 그 스킬 소관이라 이 스킬은 뜨지 않는다. 같은 조건에서 "내가 뭘 할 수 있지?", "무엇을 도와줄 수 있어?"라고 물을 때는 §3 공통 종료 출력만 실행한다. 또한 온보딩 중 Claude Code를 재시작한 사용자가 "업무 도구 연결하고 재시작했음" 또는 "git 설치하고 재시작했음"(및 유사 표현)이라고 말했을 때 §4 재시작 후 이어받기로 실행된다.
---

# onboarding-start

Terroir 온보딩의 진입점. 이 문서가 온보딩 전체 흐름의 source of truth다. 각 단계의 세부 절차는 해당 단계 스킬이 소유한다.

```
직무 확인 질문 (답을 기억해 두고, 여기서 분기하지 않음)
        ↓
공통 환경 셋팅 진행 여부 질문 (연결하기 / 건너뛰기)
        ↓ 연결하기 → env-setup   /   건너뛰기 → 바로 다음 단계
   §1의 답으로 분기 (직무를 다시 묻지 않음)
   ├─ 프로덕트 관련 직군 (개발·기획·디자인·PM 등)
   │    ├─ GitHub 계정 있음 → github-connect (조직 접근 확인, 불가면 org-access-request로 분기)
   │    │                      ↓ 완주 → §3
   │    └─ 계정 없음       → org-access-request → 대기 (승인 후 github-connect 이어받기, §3 없음)
   └─ 비 프로덕트 직군 → 바로 §3

   §3 공통 종료 출력 (시작 안내 + 커넥터 상태별 "할 수 있는 일" + 입력 안내)
```

## 진행 절차

### 1. 직무 확인

온보딩 시작 직후, 먼저 AskUserQuestion으로 직무를 묻는다. 사용자가 항상 답을 아는 질문이라 첫 단계로 부담이 없고, 이어지는 §2의 화법을 직무에 맞출 수 있다.

> 어떤 직무를 맡고 계신가요?
> 1. 프로덕트 관련 직군 — 개발, 기획, 디자인, PM 등 프로덕트를 만드는 일
> 2. 비 프로덕트 직군 — 그 외 (경영지원, 영업, 인사 등)

**여기서 바로 분기하지 않는다.** 답을 기억해 두고 §2(공통 환경 셋팅)를 먼저 진행한 뒤, §2 끝에서 이 답으로 분기한다.

### 2. 공통 환경 셋팅 (1회만)

직무와 무관하게 모두에게 필요한 도구 연결이다. AskUserQuestion으로 진행 여부를 묻되, §1에서 받은 직무에 맞춰 화법을 조정한다.

> 업무 도구 연결(지라·컨플루언스 / 이메일·캘린더·드라이브)을 지금 진행할까요?
> - **연결하기** — 도구 연결 상태를 점검하고 미연결 항목을 연결
> - **건너뛰기** — 나중에 `/env-setup`으로 연결하고, 지금은 다음 단계로

- **비 프로덕트 직군**에게는 이 연결이 온보딩의 핵심이므로 연결하기를 권한다.
- **프로덕트 관련 직군**에게는 뒤에 GitHub 연결이 이어지므로 건너뛰어도 지장 없다고 덧붙인다.

선택에 따른 처리는 직무와 무관하게 같다.

- **연결하기** → `env-setup` 스킬을 실행한다.
- **건너뛰기** → env-setup을 호출하지 않는다. 건너뛴 항목은 §3 종료 출력의 커넥터 상태 확인이 미연결로 잡아 "연결하면 할 수 있는 일"로 안내한다.

이후 어느 경로로 가든 이 단계를 다시 진행하지 않는다.

#### 2-1. 단계 종료 후 직무별 분기

env-setup은 사용자가 `/mcp`를 입력하기를 기다리며 턴을 여러 번 끊는다. 그 뒤에도 **직무를 다시 묻지 않는다** — §1의 답을 그대로 쓰되, 분기 근거를 한 문장으로 밝히고 넘어간다 (예: "프로덕트 직군이시니 GitHub 연결로 이어가겠습니다").

- **프로덕트 관련 직군** → GitHub 계정 보유 여부를 묻는다. VNTG 조직 소속 여부는 사용자에게 묻지 않는다 — github-connect의 접근 확인이 판별한다.
  - 계정 있음 (조직 소속 여부는 몰라도 됨) → `github-connect` 스킬 실행 (계정 연결 → Git 연결 → private 플러그인 접근 확인·설치, 접근 불가면 org-access-request로 자동 분기)
  - GitHub 계정 없음 → `org-access-request` 스킬 실행 (가입 지원·접근 요청)
- **비 프로덕트 직군** → GitHub·프로덕트 도구 연결은 진행하지 않는다. 바로 §3 공통 종료 출력으로 간다.

### 3. 공통 종료 출력

온보딩을 **완주한** 경로(github-connect §6 / §2-1 비 프로덕트 분기)의 종착점. 시작 안내 스킬(`terroir-*-guide:*`)이 없는 새 세션에서 "내가 무엇을 할 수 있지?" 류의 질문을 받았을 때도 앞 단계 없이 이 섹션만 실행한다 — 그 스킬이 세션에 있으면 이 질문은 그 스킬이 받는다.

**승인 대기로 끝나는 경로(`org-access-request`)에서는 실행하지 않는다** — 온보딩이 끝난 것처럼 읽힌다.

온보딩은 이 출력에서 끝난다. **작업 방법(프로젝트 시작·개발 진행)은 온보딩이 설명하지 않는다** — 시작 단계의 스타트 포인트 스킬이 답한다. 플러그인 목록 표(`claude plugin list`)도 나열하지 않는다.

아래 두 표를 순서대로 출력하고 입력 안내로 마친다 — 3-A 표가 있으면 "그 외 필요한 것이 있으면 입력하세요", 없으면 "필요한 것이 있으면 입력하세요". **3-A 자리에 표 대신 안내 한 줄이 들어간 경우도 "없으면"에 해당한다** — 기준은 표의 유무다.

#### 3-A. 시작 안내 — 로드된 스타트 포인트 표

세션에 로드된 스킬 중 **플러그인 이름**이 `terroir-*-guide` 패턴인 것이 **스타트 포인트**다 — 세션에는 `terroir-*-guide:<스킬>` 로 뜬다(예: `terroir-project-guide:project-guide`). 스킬 이름 자체에는 `guide` 가 없을 수 있으니 플러그인 쪽으로 판별한다. 아래 표로 안내한다 — 대상과 할 수 있는 일은 그 스킬의 description에서 뽑는다.

| 대상 | 할 수 있는 일 | 이렇게 시작하세요 |
| --- | --- | --- |
| {대상(소프트웨어·프로젝트 등)} | {그 스킬이 다루는 작업 단계를 `·`로 나열} | `/{스킬 이름}` |

**할 수 있는 일 열은 문장이 아니라 작업 단계 키워드를 `·`로 이어 쓴다** (예: `프로젝트 생성 · 기획 · 디자인 · 개발`). "~ 안내"처럼 스킬이 하는 행위를 적지 않는다 — 사용자가 찾는 것은 자기 일이 이 커맨드에 들어 있는지다.

3-B와 열 구성을 맞춘다 — 두 표가 "무엇 → 무엇을 할 수 있나 → 어떻게"로 같이 읽혀야 한다. 커맨드만 적으면 사용자는 그것을 왜 눌러야 하는지 모른다.

- **스타트 포인트 목록을 이 문서에 적어두지 않는다 — 조회 결과가 항상 진실이다.** 스타트 포인트가 늘어도 이 문서는 고치지 않고 행만 늘어난다.
- **로드된 스타트 포인트가 없으면 표를 생략하되, 그 자리를 어떻게 둘지는 설치 여부로 가른다.** 표가 비는 원인이 둘이라 화면도 둘이어야 한다 — 애초에 안 쓰는 사람과, 써야 하는데 세션에 안 잡힌 사람이다. 하나로 뭉치면 한쪽은 쓰지 않을 도구를 권유받고, 다른 한쪽은 무엇부터 하라는 말을 못 듣고 온보딩이 끝난다.

  먼저 `claude plugin list`로 설치 여부를 확인한다 — 3-B가 커넥터 연결을 런타임 확인하는 것과 같은 성격의 확인이다. 묻지 않고 알 수 있는 것을 묻지 않기 위한 순서다.

  - **설치돼 있는데 세션에 안 보인다** — 리로드가 안 먹었거나 설치됨+비활성 상태다. github-connect §5가 설치 후 `/reload-plugins`를 안내하므로 여기 도달했을 때는 보이는 것이 정상이다. **직무를 묻지 않는다** — private 플러그인은 프로덕트 직군만 설치하므로, 설치됐다는 사실이 곧 직무의 답이다.

    > ⬜ 프로젝트 시작 도구가 아직 이 세션에 보이지 않습니다.
    > `/reload-plugins` 를 입력해 보시고, 그래도 안 보이면 말씀해 주세요 — 설치 상태를 확인하겠습니다.

  - **설치 목록에도 없다** — 직무를 알아야 갈린다. §1의 답이 세션에 있으면 그것을 쓰고, 없으면(새 세션에서 이 섹션만 단독 실행된 경우 — §3 첫 문단) AskUserQuestion으로 §1과 같은 질문을 한 번 띄운다. **되묻는 부담보다 틀린 안내를 내는 비용이 크다** — 조건절을 붙인 한 줄("프로덕트 직군이시면 …")은 비 프로덕트 직군에게는 자기와 무관한 잡음이고, 프로덕트 직군에게는 자기를 스스로 분류하게 시키는 것이다. §1도 "사용자가 항상 답을 아는 질문이라 부담이 없다"를 근거로 같은 질문을 쓴다.

    - **비 프로덕트 직군** — 표 자리를 비운다. 3-B가 그 사람이 할 수 있는 일 전부라 종료 화면이 그것으로 완결된다. 설치를 권하지 않는다.
    - **프로덕트 관련 직군** — 아직 설치 단계까지 오지 못한 것이다. 표 자리에 아래 안내를 놓는다.

      > ⬜ 프로덕트 개발 도구가 아직 설치되지 않았습니다.
      > `/onboarding-start` 로 GitHub 연결부터 설치까지 진행할 수 있습니다.

  어느 경우든 **할 수 있는 일을 나열하지 않는다.** 목록은 스타트 포인트의 description에서 뽑는 것이고, 그것이 없어서 이 분기로 온 것이다 — 기억으로 채우면 이 문서가 사본을 갖게 된다.

#### 3-B. 할 수 있는 일 — 커넥터 상태 표

커넥터 4종의 연결 상태를 런타임 확인해(env-setup §1과 같은 방식 — 해당 서비스의 MCP 도구가 세션에 보이는지) 상태 열에 ✅(연결) / ⬜(미연결)로 표시한다.

커넥터마다 **행 3개**를 쓴다. 상태·커넥터·할 수 있는 일은 첫 행에만 적고, 나머지 두 행은 그 칸을 비운 채 예시만 채운다 — 마크다운 표 셀은 줄바꿈을 담지 못하므로 `<br>`을 쓰지 않는다.

| 상태 | 커넥터 | 할 수 있는 일 | 이렇게 말해보세요 |
| :---: | --- | --- | --- |
| ✅ 또는 ⬜ | 지라·컨플루언스 | 이슈·문서 관리 | "이번 스프린트 내 이슈 보여줘" |
| | | | "RDDP-1234 요약해줘" |
| | | | "이번 주 한 일 주간보고로 정리해줘" |
| ✅ 또는 ⬜ | 구글 메일 | 메일 검색·작성 | "안 읽은 메일 요약해줘" |
| | | | "지난주 계약 관련 메일 찾아줘" |
| | | | "이 메일 답장 초안 써줘" |
| ✅ 또는 ⬜ | 구글 캘린더 | 일정·회의실 | "내일 오후 2시 회의실 찾아줘" |
| | | | "이번 주 일정 알려줘" |
| | | | "세 명 다 되는 시간 찾아줘" |
| ✅ 또는 ⬜ | 구글 드라이브 | 자료 검색 | "작년 기획서 찾아줘" |
| | | | "이 문서 요약해줘" |
| | | | "관련 자료 모아줘" |

- **행 순서·구성은 위 표 그대로 고정한다.** 상태에 따라 정렬하거나 행을 빼지 않는다 — 볼 때마다 순서가 바뀌면 위치로 기억할 수 없고, 빠진 커넥터가 무엇인지 드러나지 않는다.
- 표 아래 안내 줄:
  - **미연결이 일부** → 미연결 커넥터를 **이름으로 불러** 안내한다. 예: "**구글 메일·구글 드라이브**는 아직 연결되지 않았습니다. `/env-setup` 으로 연결하면 바로 쓸 수 있습니다."
  - **전부 미연결** → 세 번째 열 제목을 "연결하면 할 수 있는 일"로 바꾸고, "`/env-setup` 으로 연결하면 위 기능을 쓸 수 있습니다."로 안내한다.
  - **전부 연결** → 안내 줄 없이 입력 안내로 넘어간다.
- 같은 세션의 env-setup에서 연결 완료를 확인받은 항목은 도구가 안 보여도 ✅로 적되 커넥터 이름 뒤에 `*`를 붙이고, 표 아래에 "\* 방금 연결한 항목은 다음에 Claude Code를 켤 때부터 실제로 동작합니다."를 덧붙인다 — 커넥터 목록은 세션 시작 시점에 로드되기 때문이다. **이 각주는 위 세 분기와 무관하게 `*`가 하나라도 있으면 항상 붙는다.**

출력 예 (프로덕트 직군 · 일부 연결):

> | 대상 | 할 수 있는 일 | 이렇게 시작하세요 |
> | --- | --- | --- |
> | {소프트웨어} | 프로젝트 생성 · 기획 · 디자인 · 개발 | `/terroir-{소프트웨어}-guide` |
>
> | 상태 | 커넥터 | 할 수 있는 일 | 이렇게 말해보세요 |
> | :---: | --- | --- | --- |
> | ✅ | 지라·컨플루언스 | 이슈·문서 관리 | "이번 스프린트 내 이슈 보여줘" |
> | | | | "RDDP-1234 요약해줘" |
> | | | | "이번 주 한 일 주간보고로 정리해줘" |
> | ⬜ | 구글 메일 | 메일 검색·작성 | "안 읽은 메일 요약해줘" |
> | | | | "지난주 계약 관련 메일 찾아줘" |
> | | | | "이 메일 답장 초안 써줘" |
> | ✅ | 구글 캘린더 | 일정·회의실 | "내일 오후 2시 회의실 찾아줘" |
> | | | | "이번 주 일정 알려줘" |
> | | | | "세 명 다 되는 시간 찾아줘" |
> | ⬜ | 구글 드라이브 | 자료 검색 | "작년 기획서 찾아줘" |
> | | | | "이 문서 요약해줘" |
> | | | | "관련 자료 모아줘" |
>
> **구글 메일·구글 드라이브**는 아직 연결되지 않았습니다. `/env-setup` 으로 연결하면 바로 쓸 수 있습니다.
>
> 그 외 필요한 것이 있으면 입력하세요.

출력 예 (설치됨 · 세션에 안 보임 · 커넥터 일부 연결) — 3-A 표 자리에 안내 한 줄이 들어가고, 입력 안내가 "필요한 것이 있으면"으로 바뀐다:

> ⬜ 프로젝트 시작 도구가 아직 이 세션에 보이지 않습니다.
> `/reload-plugins` 를 입력해 보시고, 그래도 안 보이면 말씀해 주세요 — 설치 상태를 확인하겠습니다.
>
> | 상태 | 커넥터 | 할 수 있는 일 | 이렇게 말해보세요 |
> | :---: | --- | --- | --- |
> | ✅ | 지라·컨플루언스 | 이슈·문서 관리 | "이번 스프린트 내 이슈 보여줘" |
> | | | | "RDDP-1234 요약해줘" |
> | | | | "이번 주 한 일 주간보고로 정리해줘" |
> | ⬜ | 구글 메일 | 메일 검색·작성 | "안 읽은 메일 요약해줘" |
> | | | | "지난주 계약 관련 메일 찾아줘" |
> | | | | "이 메일 답장 초안 써줘" |
> | ✅ | 구글 캘린더 | 일정·회의실 | "내일 오후 2시 회의실 찾아줘" |
> | | | | "이번 주 일정 알려줘" |
> | | | | "세 명 다 되는 시간 찾아줘" |
> | ⬜ | 구글 드라이브 | 자료 검색 | "작년 기획서 찾아줘" |
> | | | | "이 문서 요약해줘" |
> | | | | "관련 자료 모아줘" |
>
> **구글 메일·구글 드라이브**는 아직 연결되지 않았습니다. `/env-setup` 으로 연결하면 바로 쓸 수 있습니다.
>
> 필요한 것이 있으면 입력하세요.

**비 프로덕트 직군의 종료 화면은 위 예에서 첫 두 줄이 빠진 것이다** — 커넥터 표와 안내 줄, 입력 안내만 남는다. 그것이 이 사람의 온보딩 종착 화면으로 완결된 모습이라 따로 채우지 않는다.

### 4. 재시작 후 이어받기

온보딩 중 Claude Code를 재시작하면 대화가 끊겨 §1의 직무 답도 사라진다. 재시작을 안내한 스킬이 함께 알려준 재개 문구를 사용자가 입력하면 이 섹션이 받는다.

| 재개 문구 | 안내한 곳 | 이어받는 지점 |
| --- | --- | --- |
| "업무 도구 연결하고 재시작했음" | `env-setup` §3 (사용자가 재시작을 택한 경우) | §1 직무 확인부터 다시 진행 |
| "git 설치하고 재시작했음" | `github-connect` §1 (Windows git·gh 설치) | 직무를 묻지 않고 `github-connect` §1로 |

- **"업무 도구 연결하고 재시작했음"** → §1의 답이 사라졌으므로 직무를 다시 묻고, §2는 진행 여부를 묻지 말고 `env-setup`을 바로 실행한다. env-setup §1이 상태를 확인해 이미 연결된 항목은 건너뛴다. 이 경우에 한해 §2-1의 "직무를 다시 묻지 않는다"가 적용되지 않는다.
- **"git 설치하고 재시작했음"** → 이 문구는 프로덕트 직군이 확정된 뒤에만 나오므로 직무·환경 셋팅을 다시 묻지 않고 `github-connect` §1로 바로 넘긴다.

## 주의

- 이 스킬과 플러그인 문서에는 사내 URL·사내 이메일·private 플러그인의 이름 목록을 적지 않는다. 사용자 안내에 필요한 스타트 포인트 목록은 §3-A처럼 런타임 조회로 얻는다.
